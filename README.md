# AllSender Omnichannel Branches

Workspace-scoped branch/location routing, autonomous conversational AI and human handoff for AllSender Omnichannel.

This package is designed to plug into the existing AllSender API instead of replacing its users, workspaces, channels, AI models, customer API keys or chat assignments.

> Initial source tree is being built directly against the current AllSender host contracts.
